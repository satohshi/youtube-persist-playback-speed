# YouTube Persist Playback Speed

A userscript that makes the YouTube video playback rate persist between tabs and sessions.

## Installation

Copy the contents of `dist/index.user.js` and paste them into your preferred userscript manager.  
(Tested only on Violentmonkey, but it should work with other userscript managers.)
